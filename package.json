{
  "name": "mashangmianshi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend,admin -c blue,green,magenta \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "db:up": "docker-compose up -d postgres redis",
    "db:down": "docker-compose down",
    "db:migrate": "cd backend && alembic upgrade head",
    "setup": "npm run db:up && cd backend && pip install -r requirements.txt && cd ../frontend && npm install && cd ../admin && npm install",
    "build": "cd frontend && npm run build && cd ../admin && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
